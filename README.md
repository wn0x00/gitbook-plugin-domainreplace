# gitbook-plugin-domainreplace

{
    "plugins" : ["domainreplace"]
    "pluginsConfig": {
        "domainreplace":{
        "pattern": "xxxxxxxxxxxxxxxxxxx",
        "flags":"g",
        "substitute":""
      }
    }
}
